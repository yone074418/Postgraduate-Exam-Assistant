# AGENTS.md

# 1. 项目概述

- 项目名称：
  - 考研 AI 助手

- 项目类型：
  - 纯前端 Web 应用
  - 不开发独立后端
  - 不使用服务端数据库
  - 用户数据主要保存在浏览器本地
  - AI 能力通过调用 `deepseekv4flash` API 实现

- 产品目标：
  - 帮助考研学生进行院校推荐
  - 帮助考研学生推荐课程和资料
  - 帮助用户生成学习计划
  - 帮助用户安排每日学习任务
  - 帮助用户进行每日打卡和学习复盘
  - 帮助用户保持健康作息和稳定备考节奏

- 核心功能：
  - 用户考研信息填写
  - AI 聊天问答
  - 院校推荐
  - 课程与资料推荐
  - 学习计划生成
  - 每日任务安排
  - 每日打卡
  - 学习复盘
  - 健康作息建议
  - 本地数据管理

- 技术方向：
  - 前端使用 Vue 3 + Vite + TypeScript
  - 状态管理使用 Pinia
  - 路由管理使用 Vue Router
  - 样式使用 Tailwind CSS
  - 数据存储使用 localStorage / IndexedDB
  - AI 调用使用 deepseekv4flash API

# 2. 开发规范

- 项目开发原则：
  - 优先保证功能简单可用
  - 不做过度设计
  - 页面结构要清晰
  - 功能入口不要太多
  - 重点突出今日任务、学习计划和 AI 辅助能力
  - 所有功能以 MVP 可落地为优先

- 纯前端限制：
  - 不新增后端服务
  - 不新增服务端数据库
  - 不设计复杂登录注册系统
  - 不实现云端同步
  - 不把 API Key 写死在代码中
  - 不把用户隐私信息上传到无关接口

- 页面开发规范：
  - 每个主要功能使用独立页面
  - 页面组件放在 `pages` 目录
  - 可复用组件放在 `components` 目录
  - 接口请求统一放在 `api` 目录
  - 状态管理统一放在 `stores` 目录
  - 工具函数统一放在 `utils` 目录
  - 类型定义统一放在 `types` 目录

- 推荐项目结构：

```txt
src
├── api
│   └── aiService.ts
├── assets
│   └── styles
│       └── main.css
├── components
│   ├── common
│   │   ├── BottomNav.vue
│   │   └── EmptyState.vue
│   ├── chat
│   │   ├── ChatMessage.vue
│   │   └── QuickQuestionList.vue
│   ├── task
│   │   └── TaskCard.vue
│   ├── school
│   │   └── SchoolCard.vue
│   └── course
│       └── CourseCard.vue
├── pages
│   ├── Home.vue
│   ├── Chat.vue
│   ├── ProfileForm.vue
│   ├── SchoolRecommend.vue
│   ├── CourseRecommend.vue
│   ├── StudyPlan.vue
│   ├── Checkin.vue
│   └── UserCenter.vue
├── router
│   └── index.ts
├── stores
│   ├── userStore.ts
│   ├── chatStore.ts
│   ├── taskStore.ts
│   ├── planStore.ts
│   └── settingStore.ts
├── types
│   └── index.ts
├── utils
│   ├── storage.ts
│   ├── prompt.ts
│   ├── date.ts
│   └── markdown.ts
├── App.vue
└── main.ts
```

- API 调用规范：
  - 所有 AI 请求必须通过 `api/aiService.ts` 统一封装
  - 页面组件中不要直接写 fetch / axios 请求
  - 请求前需要判断 API Key 是否存在
  - 请求中需要显示 loading 状态
  - 请求失败需要显示错误提示
  - 支持重新生成
  - 防止重复点击导致多次请求

- Prompt 管理规范：
  - Prompt 统一放在 `utils/prompt.ts`
  - 页面中不要直接写大段 Prompt
  - 不同功能使用不同 Prompt 模板
  - Prompt 需要结合用户填写的考研信息
  - AI 输出应尽量简洁、清晰、可执行

- 本地存储规范：
  - 用户基础信息可以存入 localStorage
  - API Key 可以存入 localStorage
  - 聊天记录、学习计划、每日任务、复盘记录可以使用 IndexedDB
  - MVP 阶段可以全部先用 localStorage
  - 本地存储读写需要统一封装在 `utils/storage.ts`
  - 清空本地数据前必须二次确认

# 3. 测试要求

- 基础功能测试：
  - 用户可以填写并保存考研基础信息
  - 页面刷新后用户信息仍然存在
  - 用户可以修改个人信息
  - 用户可以正常进入首页、聊天页、推荐页、计划页、打卡页和个人中心页

- AI 调用测试：
  - 未填写 API Key 时，需要提示用户先设置 API Key
  - API Key 填写后，可以正常调用 `deepseekv4flash` API
  - AI 请求过程中显示加载状态
  - AI 请求失败时显示错误提示
  - AI 返回内容为空时提示重新生成
  - 连续点击生成按钮时不能重复发起多个请求

- 院校推荐测试：
  - 可以根据用户信息生成院校推荐
  - 推荐结果需要包含冲刺、稳妥、保底分类
  - 推荐结果需要展示推荐理由、风险提示和备考建议
  - 推荐结果可以保存到本地

- 课程推荐测试：
  - 可以根据用户基础生成课程和资料推荐
  - 推荐内容需要包含科目、适合人群、推荐理由和使用方法
  - 推荐结果可以保存到本地

- 学习计划测试：
  - 可以根据用户信息生成学习计划
  - 学习计划需要包含阶段目标、每周计划和每日任务
  - 用户可以保存学习计划
  - 用户可以重新生成学习计划

- 每日任务测试：
  - 用户可以查看今日任务
  - 用户可以新增任务
  - 用户可以修改任务
  - 用户可以删除任务
  - 用户可以标记任务完成状态
  - 页面刷新后任务数据不能丢失

- 打卡复盘测试：
  - 用户可以填写学习状态
  - 用户可以填写学习时长
  - 用户可以填写未完成原因
  - AI 可以根据任务完成情况生成复盘建议
  - 复盘记录可以保存到本地

- 移动端测试：
  - 页面在手机宽度下显示正常
  - 底部导航栏可正常点击
  - 聊天输入框在移动端可正常使用
  - 任务卡片和推荐卡片不应过于拥挤
  - 按钮大小适合手指点击

# 4. 代码风格

- 基础风格：
  - 使用 TypeScript
  - 变量命名使用小驼峰
  - 组件命名使用大驼峰
  - 文件命名保持清晰，能表达功能含义
  - 不写无意义变量名
  - 不写过长函数
  - 不把多个复杂功能写在同一个组件里

- Vue 组件规范：
  - 推荐使用 `<script setup lang="ts">`
  - 页面组件只负责页面展示和简单交互
  - 复杂逻辑拆到 store、utils 或 api 中
  - 可复用 UI 拆成 components
  - 组件 props 需要定义类型
  - 组件事件命名要清晰

- TypeScript 规范：
  - 重要数据结构必须定义 interface 或 type
  - 不滥用 any
  - API 返回数据需要定义类型
  - Store 中的数据也需要定义类型

- 样式规范：
  - 优先使用 Tailwind CSS
  - 页面保持简洁
  - 不使用过多颜色
  - 卡片、按钮、输入框风格保持统一
  - 移动端优先适配
  - 页面不要堆太多内容

- 状态管理规范：
  - 用户信息放在 `userStore`
  - 聊天记录放在 `chatStore`
  - 学习计划放在 `planStore`
  - 每日任务放在 `taskStore`
  - API Key 和系统配置放在 `settingStore`
  - Store 中只放全局需要共享的数据

- 注释规范：
  - 复杂逻辑需要写注释
  - Prompt 模板需要说明用途
  - API 请求方法需要说明参数和返回值
  - 不需要给简单代码写无意义注释

# 5. 注意事项

- 关于 API Key：
  - 纯前端项目无法彻底隐藏 API Key
  - 不要把 API Key 写死在源码中
  - 用户可以在个人中心手动填写 API Key
  - 页面需要提示用户纯前端调用 API 存在 Key 暴露风险
  - 如果后续正式上线，建议增加后端代理接口保护 API Key

- 关于用户隐私：
  - 用户数据默认保存在本地浏览器
  - 不要上传用户隐私信息到无关服务
  - 调用 AI 时只发送必要的用户信息和问题内容
  - 提醒用户不要输入身份证号、手机号、住址等敏感信息
  - 用户需要可以清空本地数据

- 关于 AI 内容：
  - AI 推荐的院校、分数线、招生人数等信息可能不准确
  - 页面需要提示用户以学校官网和研招网信息为准
  - AI 健康作息建议只作为普通生活建议
  - 涉及严重焦虑、失眠等情况时，需要提醒用户寻求专业帮助
  - 不要把 AI 输出当作绝对正确结果

- 关于数据可靠性：
  - 页面刷新后，本地数据不能丢失
  - 重要数据修改前需要确认
  - 清空数据前必须弹窗确认
  - 本地存储失败时需要提示用户
  - 网络异常时需要保留用户输入内容

- 关于功能范围：
  - MVP 阶段只做纯前端版本
  - 不做登录注册
  - 不做服务端数据库
  - 不做云端同步
  - 不做社区、排行榜、好友监督
  - 不做复杂日历
  - 不做真实院校数据库
  - 后续版本再考虑扩展这些功能

# 6. 开发优先级

- 第一阶段：
  - 搭建 Vue 3 + Vite + TypeScript 项目
  - 配置 Vue Router
  - 配置 Pinia
  - 配置 Tailwind CSS
  - 搭建首页、聊天页、个人中心页基础结构

- 第二阶段：
  - 完成用户信息填写
  - 完成本地存储封装
  - 完成 API Key 设置
  - 完成个人中心信息修改

- 第三阶段：
  - 封装 `deepseekv4flash` API
  - 完成 Prompt 模板
  - 完成 AI 聊天页
  - 完成聊天记录保存

- 第四阶段：
  - 完成院校推荐
  - 完成课程推荐
  - 完成学习计划生成
  - 支持保存 AI 生成结果

- 第五阶段：
  - 完成每日任务
  - 完成任务新增、修改、删除
  - 完成任务打卡
  - 完成 AI 复盘
  - 完成健康作息建议

- 第六阶段：
  - 优化移动端体验
  - 优化错误提示
  - 优化 Markdown 渲染
  - 优化本地数据管理

# 7. 给开发 Agent 的要求

- 修改代码前先理解 PRD 和 TECH_DESIGN
- 不要擅自引入后端
- 不要擅自引入复杂数据库
- 不要写死 API Key
- 不要删除已有核心功能
- 不要破坏本地数据结构
- 不要把大段 Prompt 写进页面组件
- 不要让页面功能过于复杂
- 保持代码清晰、可维护、易扩展
- 优先完成 MVP 核心闭环

# 8. 核心闭环

- 用户填写考研信息
- AI 推荐院校
- AI 推荐课程和资料
- AI 生成学习计划
- 系统生成每日任务
- 用户完成任务并打卡
- AI 生成每日复盘
- 系统辅助用户调整后续学习安排